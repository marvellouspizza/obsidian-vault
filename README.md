# Obsidian Vault

此仓库为 Obsidian 笔记库的 Git 同步仓库。

使用步骤：

- 本地已初始化并包含 `.gitignore`。
- 如果需要我可以为你在 GitHub 上创建远程仓库并推送（需要授权）。

如果要我创建并推送远程仓库，请回复：
1) 远程仓库名（例如 `obsidian-vault`）
2) 是否公开（`public` / `private`）。
